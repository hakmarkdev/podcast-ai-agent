# Environment variables for Podcast Summary Agent

# OpenAI API Key (if using OpenAI Whisper API instead of local model)
# OPENAI_API_KEY=sk-...

# Default output directory (overrides config/default.yaml)
# OUTPUT_DIR=./output

# Default Whisper model (overrides config/default.yaml)
# WHISPER_MODEL=base

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Proxy settings (if needed)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
